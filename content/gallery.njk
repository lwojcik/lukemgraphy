---
layout: base
pagination:
    data: apiData.galleries
    size: 1
    alias: gallery
    addAllPagesToCollections: true
permalink: "{{ gallery.link }}"
eleventyComputed:
  title: "{{ gallery.name }}"
  ogImage: "{{ gallery.cover }}"
  ogUrl: "{{ gallery.link }}"
  description: "{{ gallery.name }} - photography gallery by {{ siteConfig.author.name }}."
---

{% include "partials/gallery-view.njk" %}